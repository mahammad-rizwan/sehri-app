# ─── Server ────────────────────────────────────────────────────────────────
NODE_ENV=development
PORT=5000

# ─── Database (MySQL) ──────────────────────────────────────────────────────
DB_HOST=localhost
DB_PORT=3306
DB_NAME=sehri_connect
DB_USER=root
DB_PASSWORD=

# ─── JWT ───────────────────────────────────────────────────────────────────
JWT_SECRET=
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=
JWT_REFRESH_EXPIRES_IN=30d

# ─── OTP — MessageCentral VerifyNow ────────────────────────────────────────
# Primary credentials
MESSAGECENTRAL_CUSTOMER_ID=
MESSAGECENTRAL_PASSWORD=
# Optional backup credentials, tried automatically if the primary pair fails
MESSAGECENTRAL_CUSTOMER_ID1=
MESSAGECENTRAL_PASSWORD1=

# ─── Donation proof storage ────────────────────────────────────────────────
# Railway's disk is ephemeral — without these, uploaded proofs are lost on
# every redeploy. Set either CLOUDINARY_URL, or the three separate values.
CLOUDINARY_URL=
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=

# ─── CORS / Socket.IO origin in production ─────────────────────────────────
FRONTEND_URL=

# ─── Super Admin (read by `npm run seed`; no defaults, seeding fails without) ─
SUPER_ADMIN_PHONE=
SUPER_ADMIN_NAME=
SUPER_ADMIN_PASSWORD=

# ─── Test fixtures (optional, dev only — see src/database/seed-test-users.js) ─
# TEST_USER_PASSWORD=
# TEST_ADMIN_PASSWORD=

# Google Routes API — SERVER key, used only when a super admin presses
# "Regenerate Map Path" (about 2 requests per press, not per map view).
# Must NOT be the Maps SDK key from mobile/app.json: that one ships inside the
# APK. Create a separate key restricted to "Routes API" + this server's IP.
# Leave empty and paths are drawn as straight lines between stops instead.
GOOGLE_ROUTES_API_KEY=
